<%= form_for(@kuaibovideo) do |f| %>
  <% if @kuaibovideo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kuaibovideo.errors.count, "error") %> prohibited this kuaibovideo from being saved:</h2>

      <ul>
      <% @kuaibovideo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
