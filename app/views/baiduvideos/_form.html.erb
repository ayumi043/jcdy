<%= form_for(@baiduvideo) do |f| %>
  <% if @baiduvideo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baiduvideo.errors.count, "error") %> prohibited this baiduvideo from being saved:</h2>

      <ul>
      <% @baiduvideo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
